---
title: "GET /v1/workbook-installations/{workbook_installation_id}/provenance"
description: "Return the durable workbook provenance ledger: latest fingerprint, context, active mappings, refresh history, and audit events."
---

Return the durable workbook provenance ledger: latest fingerprint, context, active mappings, refresh history, and audit events.

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/workbook-installations/example-workbook-installation-id/provenance"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "workbook_provenance_ledger",
  "provenance": {
    "workbookInstallation": {
      "workbookInstallationId": "wb_install_example",
      "organizationId": "org_example",
      "host": "excel_mac",
      "workbookIdentityKey": "office:drive-item:example",
      "workbookFingerprintHash": "sha256:fingerprint",
      "installationVersion": 1,
      "installedAt": "2026-08-16T12:00:00.000Z"
    },
    "latestFingerprint": null,
    "latestModelContextSnapshot": null,
    "mappings": [],
    "refreshRuns": [],
    "applyReceipts": [],
    "recoveryCases": [],
    "auditEvents": []
  },
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/workbook-installations/{workbook_installation_id}/provenance to return the durable workbook provenance ledger: latest fingerprint, context, active mappings, refresh history, and audit events. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
