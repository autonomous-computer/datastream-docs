---
title: "POST /v1/workbook-installations/{workbook_installation_id}/model-context-snapshots"
description: "Capture workbook model context"
---

Capture workbook model context

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

Capture the workbook model context used for preview, approval, and safe apply checks.

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X POST \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  -H "content-type: application/json" \
  -d '{}' \
  "https://api.secapi.ai/v1/workbook-installations/example-workbook-installation-id/model-context-snapshots"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "model_context_snapshot",
  "modelContextSnapshot": {
    "modelContextSnapshotId": "model_ctx_example",
    "workbookInstallationId": "wb_install_example",
    "workbookContentHash": "sha256:content",
    "workbookFingerprintHash": "sha256:fingerprint",
    "sheetManifestHash": "sha256:sheets",
    "mappingVersion": 1,
    "capturedAt": "2026-08-16T12:00:00.000Z",
    "host": "excel_mac"
  },
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API POST /v1/workbook-installations/{workbook_installation_id}/model-context-snapshots to capture workbook model context. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
