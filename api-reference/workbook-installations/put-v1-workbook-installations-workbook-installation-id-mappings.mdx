---
title: "PUT /v1/workbook-installations/{workbook_installation_id}/mappings"
description: "Save append-only workbook mapping versions for audited filing-to-model refreshes."
---

Save append-only workbook mapping versions for audited filing-to-model refreshes.

<Info>
Audience: application and coding agent.
</Info>

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X PUT \
  -H "x-api-key: $SECAPI_API_KEY" \
  -H "secapi-version: 2026-03-19" \
  -H "content-type: application/json" \
  -d '{}' \
  "https://api.secapi.ai/v1/workbook-installations/example-workbook-installation-id/mappings"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "object": "model_mapping_list",
  "mappings": [],
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API PUT /v1/workbook-installations/{workbook_installation_id}/mappings to save append-only workbook mapping versions for audited filing-to-model refreshes. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
