---
title: "GET /v1/embed/owners/13f/manager/{cik}"
description: "13F manager profile"
---

13F manager profile

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

Anonymous member of `/v1/embed/owners/13f/managers`: manager profile, curated bio when available, and the indexed filings list (quarter, holdings count, total value, top holdings). Returns 404 while this public embed is not enabled or when no filings are indexed for the CIK.

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/embed/owners/13f/manager/example-cik"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/embed/owners/13f/manager/{cik} to 13F manager profile. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
