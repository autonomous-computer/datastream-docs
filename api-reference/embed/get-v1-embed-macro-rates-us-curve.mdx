---
title: "GET /v1/embed/macro/rates/us-curve"
description: "US Treasury yield curve"
---

US Treasury yield curve

<Info>
Audience: application and coding agent.
</Info>

## Coverage and source scope

Anonymous, cacheable US Treasury yield curve built from FRED-derived public-domain observations: latest, month-ago, and year-ago curve snapshots across the 2Y, 5Y, 7Y, 10Y, 20Y, and 30Y tenors; 10Y-2Y and 10Y-3M spread snapshots; and bounded weekly history per series. Returns 404 while this public embed is not enabled.

## Canonical metadata

- `requestId`
- `traceparent`

## Example request

<RequestExample>
```bash
curl -X GET \
  -H "secapi-version: 2026-03-19" \
  "https://api.secapi.ai/v1/embed/macro/rates/us-curve"
```
</RequestExample>

## Example response

<ResponseExample>
```json
{
  "requestId": "req_2ZK8Q1W9F4M6P7R3"
}
```
</ResponseExample>

## Give this prompt to your agent

<Prompt>
Use SEC API GET /v1/embed/macro/rates/us-curve to uS Treasury yield curve. Preserve `requestId`, `traceparent` in the output. Return a concise markdown summary plus the exact structured payload fields that a downstream engineer or agent should keep using this result.
</Prompt>

## Failure posture

- treat non-2xx responses as contract-aware failures, not free-form errors
- preserve `requestId` and `traceparent` in logs and downstream reports
- if provenance or freshness metadata is present, return it unchanged so trust is not lost in the handoff
